apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: nexacore
  labels:
    app: notifications-service
spec:
  selector:
    app: notifications-service
  ports:
    - protocol: TCP
      port: 3005
      targetPort: 3005
  type: ClusterIP