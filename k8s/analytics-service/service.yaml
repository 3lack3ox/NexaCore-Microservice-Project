apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: nexacore
  labels:
    app: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 3006
      targetPort: 3006
  type: ClusterIP