apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: nexacore
  labels:
    app: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - protocol: TCP
      port: 3007
      targetPort: 3007
  type: ClusterIP